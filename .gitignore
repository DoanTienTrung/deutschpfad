# Environment variables (chứa secret thật, không commit)
.env
cookies.txt

# DB dumps (chứa dữ liệu user thật -- email, password hash, refresh token)
*.sql

# Let's Encrypt cert files + ACME challenge webroot (chỉ tồn tại trên server, không commit)
certbot-etc/
certbot-webroot/

# Backend (Maven/Java)
backend/target/
backend/.mvn/wrapper/maven-wrapper.jar
*.class

# Frontend (Node/Vite)
frontend/node_modules/
frontend/dist/

# IDE
.idea/
*.iml
.vscode/

# OS
.DS_Store
Thumbs.db

# AI tool configs (local-only, not shared via repo)
.agents/
.claude/
.cursor/
.gemini/
.kiro/
.codex/
.impeccable/

# Raw seed data (already imported into the DB, not needed in the repo)
scripts/
English-Deutsch (Sorted by Frequency).txt

# Internal planning docs (not shared via repo)
docs/
